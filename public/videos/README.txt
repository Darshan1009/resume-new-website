DROP YOUR BACKGROUND VIDEOS HERE
================================

The site auto-detects these filenames and fades them in behind each section:

  overview.mp4       — shown on section 01 // OVERVIEW
  breach-logs.mp4    — shown on section 02 // BREACH LOGS
  cyber-labs.mp4     — shown on section 03 // CYBER LABS
  arsenal.mp4        — shown on section 04 // ARSENAL

If a file is missing, the section falls back to the matrix grid plus a
tinted radial glow — no errors thrown.

WHERE TO GET CLIPS (free, no signup required):
  • https://www.pexels.com/videos/  — search terms below

Suggested searches per section:
  overview     → "abstract digital", "data flow", "dark code"
  breach-logs  → "alert red glitch", "warning system", "binary scrolling"
  cyber-labs   → "matrix code looping", "green digital grid", "code rain"
  arsenal      → "circuit board", "network nodes", "particle grid"

Tips:
  • Keep clips short (5-15s) and seamlessly loopable
  • 720p is plenty (1080p adds weight without much visual gain)
  • The site applies opacity 0.45 + dark gradient overlays, so darker
    source footage looks better than bright clips
